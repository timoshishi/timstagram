import { createImage } from './cropper-functions';

describe('createImage', () => {
  it('times out if given an invalid URL', async () => {
    // console.log(await createImage('werewqa'));
    expect(true).toBeTruthy();
    // expect(result).toThrow();
  });
});
